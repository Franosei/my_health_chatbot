target-version = "py312"

[lint]
# Keep CI deterministic instead of inheriting a changing Ruff default rule set.
# Additional families such as UP and BLE should be enabled incrementally after
# their existing findings have been reviewed and fixed in dedicated changes.
select = ["E4", "E7", "E9", "F"]
